name: Apply approved album proposal

# Triggered by review_app (github_dispatch.py) when an approver clicks "Upload & Publish"
# on an approved album proposal (see GitHub issue #20), or automatically via AI approval.
#
# Only the proposal id crosses into this workflow; metadata and audio/cover files
# are fetched from review_app's authenticated callback API using
# secrets.REVIEW_APP_CALLBACK_KEY.
#
# Media is uploaded directly to archive.org using secrets.IA_ACCESS_KEY_ID /
# secrets.IA_SECRET_ACCESS_KEY, and YAML metadata changes are committed to main.

on:
  workflow_dispatch:
    inputs:
      proposal_id:
        description: >-
          A specific approved album proposal's id to apply. Leave blank to sweep
          every currently-approved album proposal instead.
        required: false
        type: string
  schedule:
    # Self-healing sweep every 15 minutes for dropped dispatches
    - cron: "*/15 * * * *"

permissions:
  contents: write
  actions: write

concurrency:
  group: apply-archive-mutations
  cancel-in-progress: false

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install -r tools/requirements.txt
          which ffprobe || (sudo apt-get update && sudo apt-get install -y ffmpeg)

      - name: Determine which album proposal(s) to apply
        id: targets
        env:
          INPUT_PROPOSAL_ID: ${{ github.event.inputs.proposal_id }}
        run: |
          if [ -n "$INPUT_PROPOSAL_ID" ]; then
            if ! [[ "$INPUT_PROPOSAL_ID" =~ ^[0-9]+$ ]]; then
              echo "::error::Invalid proposal_id format (must be an integer)"
              exit 1
            fi
            echo "ids=$INPUT_PROPOSAL_ID" >> "$GITHUB_OUTPUT"
          else
            ids=$(curl -fsSL \
              -H "Authorization: Bearer ${{ secrets.REVIEW_APP_CALLBACK_KEY }}" \
              "${{ vars.REVIEW_APP_BASE_URL }}/api/album-proposals/approved" \
              | python3 -c "import json,sys; print(' '.join(str(i) for i in json.load(sys.stdin)))")
            echo "ids=$ids" >> "$GITHUB_OUTPUT"
          fi

      - name: Apply each album proposal
        if: steps.targets.outputs.ids != ''
        shell: bash --noprofile --norc -o pipefail {0}
        env:
          PROPOSAL_IDS: ${{ steps.targets.outputs.ids }}
          IA_ACCESS_KEY_ID: ${{ secrets.IA_ACCESS_KEY_ID }}
          IA_SECRET_ACCESS_KEY: ${{ secrets.IA_SECRET_ACCESS_KEY }}
        run: |
          git config user.name "daugavpils-fans-bot"
          git config user.email "daugavpils@gmail.com"
          overall_failed=0
          for id in $PROPOSAL_IDS; do
            if ! [[ "$id" =~ ^[0-9]+$ ]]; then
              echo "::error::invalid proposal id format: $id"
              overall_failed=1
              continue
            fi
            echo "::group::Album proposal #$id"
            success=0
            error=""

            rm -rf staged_tracks proposal.json cover_file.bin
            mkdir -p staged_tracks

            if curl -fsSL \
                 -H "Authorization: Bearer ${{ secrets.REVIEW_APP_CALLBACK_KEY }}" \
                 "${{ vars.REVIEW_APP_BASE_URL }}/api/album-proposals/$id" \
                 -o proposal.json; then

              has_cover=$(python3 -c "import json; print('true' if json.load(open('proposal.json')).get('has_cover') else 'false')")
              cover_arg=""
              if [ "$has_cover" = "true" ]; then
                curl -fsSL \
                  -H "Authorization: Bearer ${{ secrets.REVIEW_APP_CALLBACK_KEY }}" \
                  "${{ vars.REVIEW_APP_BASE_URL }}/api/album-proposals/$id/cover" \
                  -o cover_file.bin
                cover_arg="--cover-file cover_file.bin"
              fi

              track_positions=$(python3 -c "import json; print(' '.join(str(t['position']) for t in json.load(open('proposal.json')).get('tracks', [])))")
              download_failed=0
              for pos in $track_positions; do
                if ! curl -fsSL \
                     -H "Authorization: Bearer ${{ secrets.REVIEW_APP_CALLBACK_KEY }}" \
                     "${{ vars.REVIEW_APP_BASE_URL }}/api/album-proposals/$id/tracks/$pos/file" \
                     -o "staged_tracks/track_${pos}.bin"; then
                  echo "::error::failed to download track $pos for proposal #$id"
                  download_failed=1
                  break
                fi
              done

              if [ "$download_failed" = "0" ] && python tools/apply_album_proposal.py --proposal-file proposal.json --tracks-dir staged_tracks $cover_arg; then
                git add -A -- bands
                if git diff --cached --quiet; then
                  success=1
                else
                  python tools/format_commit_message.py --is-album --proposal-file proposal.json --output commit_msg.txt --summary-file "${GITHUB_STEP_SUMMARY:-}"
                  git commit -F commit_msg.txt
                  pushed=0
                  for attempt in 1 2 3 4 5; do
                    if git push origin main; then
                      pushed=1
                      break
                    fi
                    echo "push rejected (attempt $attempt/5) - fetching and rebasing before retrying"
                    git fetch origin main
                    git rebase origin/main
                    sleep $((RANDOM % 4 + 1))
                  done
                  if [ "$pushed" = "1" ]; then
                    success=1
                  else
                    error="git push kept getting rejected after 5 attempts"
                  fi
                fi
              else
                error="apply_album_proposal failed - see the Action run logs"
              fi
            else
              error="failed to fetch album proposal metadata from review_app"
            fi

            if [ "$success" = "1" ]; then
              payload="{\"success\": true, \"run_id\": \"${{ github.run_id }}\"}"
            else
              echo "::error::album proposal #$id failed: $error"
              payload="{\"success\": false, \"run_id\": \"${{ github.run_id }}\", \"error\": \"$error\"}"
              overall_failed=1
            fi

            curl -fsSL -X POST \
              -H "Authorization: Bearer ${{ secrets.REVIEW_APP_CALLBACK_KEY }}" \
              -H "Content-Type: application/json" \
              -d "$payload" \
              "${{ vars.REVIEW_APP_BASE_URL }}/api/album-proposals/$id/result"

            rm -rf staged_tracks proposal.json cover_file.bin
            echo "::endgroup::"
          done
          exit $overall_failed

      - name: Trigger Pages mirror rebuild
        if: always() && steps.targets.outputs.ids != ''
        run: gh workflow run pages.yml --ref main
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Trigger metadata sync to archive.org
        if: always() && steps.targets.outputs.ids != ''
        run: gh workflow run sync-metadata.yml --ref main
        env:
          GH_TOKEN: ${{ github.token }}
