.venv/
__pycache__/
*.pyc

# Audio/image/video media files are deliberately kept out of git (see
# README: "What's actually portable here" / distribution model). They live
# locally, and are distributed separately as checksummed torrent releases.
bands/**/*.mp3
bands/**/*.flac
bands/**/*.wav
bands/**/*.ogg
bands/**/*.m4a
bands/**/*.jpg
bands/**/*.jpeg
bands/**/*.png
bands/**/*.webp
bands/**/*.mp4
bands/**/*.mov
bands/**/*.mkv

# Generated static site output (ADR-0001) - a build artifact, not source.
webapp/dist/
